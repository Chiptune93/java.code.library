public class Test {
    public static void main(String[] args) {
        // what is GCD of 10,5 -> 5
        System.out.println(EuclideanAlgorithm.GCD.recursion(10, 5));
        // what is LCM of 10,5 -> 10
        System.out.println(EuclideanAlgorithm.LCM.lcm(10, 5));
    }
}
